# Edusentia r37 Login Hero v5 Deployment

Production visual correction marker.

- Nipe activates `r37-final-ui-5` plus its local canonical hero wrapper.
- The wrapper imports the Master self-contained campus hero stylesheet from the same `nduah89.github.io` origin.
- Service-worker cache generation is `v7-4-0-r37-edusentia-final-ui-5`.
- The old external WebP login-hero dependency is no longer in the active service-worker asset list.
- Nipe school identity, authentication IDs, MFA and signed licence bindings are unchanged.

This marker exists to force and identify the final GitHub Pages deployment containing the complete v5 visual correction.
